# =============================================================================
# ACQUISITIONS - Environment Configuration Template
# =============================================================================
# Copy this file to:
#   - .env.development  (for local development with Neon Local)
#   - .env.production   (for production deployment)
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3000
# Options: 'development' | 'production'
NODE_ENV=development
# Options: 'debug' | 'info' | 'warn' | 'error'
LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Connection string format:
#   Development: postgres://neondb_owner:password@db:5432/neondb
#   Production:  postgres://user:pass@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
NEON_URI=

# Set to 'true' ONLY for local development with Neon Local proxy
# This enables HTTP mode instead of WebSocket mode
NEON_LOCAL=false

# -----------------------------------------------------------------------------
# Neon Local Configuration (Development Only)
# These are NOT required in production
# Get values from: https://console.neon.tech/
# -----------------------------------------------------------------------------
# API Key: Account Settings → API Keys
NEON_API_KEY=
# Project ID: Project Settings → General
NEON_PROJECT_ID=
# Branch ID: Project → Branches → Copy ID of main branch
PARENT_BRANCH_ID=
# Database name in your Neon project
DB_NAME=neondb
# Delete ephemeral branch when container stops
DELETE_BRANCH=true

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# Secret key for signing tokens
# Generate with: openssl rand -base64 32
JWT_SECRET=
# Token expiration (e.g., 1d, 7d, 1h)
JWT_EXPIRES_IN=1d

# -----------------------------------------------------------------------------
# Arcjet Configuration (Rate Limiting & Security)
# Get your key from: https://app.arcjet.com/
# -----------------------------------------------------------------------------
ARCJET_KEY=
